<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FlickrBrowser">
        <activity
            android:name=".BrowserView"
            android:exported="false"
            android:label="@string/title_activity_browser_view"
            android:theme="@style/Theme.FlickrBrowser" />
        <activity
            android:name=".TagsView"
            android:exported="false"
            android:label="@string/title_activity_tags_view"
            android:theme="@style/Theme.FlickrBrowser" />
        <activity
            android:name=".RequesterView"
            android:exported="false"
            android:label="@string/title_activity_requester_view"
            android:theme="@style/Theme.FlickrBrowser" />
        <activity
            android:name=".ResultsActivity"
            android:exported="false"
            android:label="@string/title_activity_results"
            android:theme="@style/Theme.FlickrBrowser" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.FlickrBrowser">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>